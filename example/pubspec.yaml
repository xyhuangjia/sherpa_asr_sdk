name: example
description: "Sherpa ASR SDK Example App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sherpa_asr_sdk:
    path: ../
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  just_audio: ^0.9.36
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
